import Head from 'next/head';

const ListHeader = () => {
    return (
        <Head>
            <title>Tasks | Manage Tasks</title>
            <meta name="keywords" content="personal tasks" />
        </Head>
    );
}

export default ListHeader;