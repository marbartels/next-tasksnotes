{/* <div className="tooltip-origin">
                <span className="tooltiptext">task.urgency</span>
            </div>

            <div className={'todo-item task-normal rounded-left lightblue-bg list-txt-color' + todoStyling.item}>

                <li>{task.name}</li>

                <form className="descr-edit-form hidden edit-form-id" onSubmit={handleEdit}>
                    <input
                        type="text"
                        required
                        value={descrEdit}
                        list={listId}
                        task={task._id}
                        className="editing-descr-input unit-ht lightblue-bg no-highlight"
                        onChange={(e) => setDescrEdit(e.target.value)}
                        onClick={toggleEdit}
                    />
                </form>
                <div className="edit-task">
                    <img className="edit-descr-img" src="https://img.icons8.com/plumpy/48/000000/text-cursor.png" />
                    <img className="edit-descr-img hidden" src="/icons/icons8-checkmark-64.png" />
                </div>

            </div>

            <div className={"complete-btn-wrapper cbtn-div-positioning" + todoStyling.complete}>
                <input type="text"
                    list={listId}
                    slug={currentSlug}
                    task={task._id}
                    className="todo-complete-btn"
                    completed={task.complete ? "yes" : "no"}
                    onClick={completeHandler}
                />
                <i className={todoStyling.completeIcon}></i>
            </div>

            <div className={"trash-btn-wrapper cbtn-div-positioning" + todoStyling.trash}>
                <input type="checkbox"
                    list={listId}
                    slug={currentSlug}
                    task={task._id}
                    onClick={trashHandler}
                    className="todo-rmv-btn"
                />
                <i className="fas fa-trash"></i>
            </div> */}
